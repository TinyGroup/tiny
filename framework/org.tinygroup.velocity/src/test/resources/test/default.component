#macro(add1 $list)
#set($total=0)
#foreach($item in $list)
$item
#set($total =$total+$item)
#end
--------------
total = $total
#end

#macro(d1)
<tr><td>$!bodyContent</td></tr>
#end


#macro(accordion1 $id) 
<div id="$id">
 $!bodyContent
</div>
<script>
$(function() {
    $( "#$id" ).accordion();
});
</script>
#end

#macro(accordionSection1 $title)
   <h3>$!title</h3>
    <div>
       $!bodyContent
    </div>
#end
